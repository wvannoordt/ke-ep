#include <print.h>

#include "dual.h"

template <typename val_t>
using vec5_t = std::array<val_t, 5>;
template <typename val_t>
using mat5_t = std::array<vec5_t, 5>;
using real_t = dual_t<double>;

inline vec5_t<real_t>
flx_w(const vec5_t<real_t>& w, int idir)
{
    vec5_t<real_t> output;
    
    return output;
}

int main(int argc, char** argv)
{
    
    return 0;
}